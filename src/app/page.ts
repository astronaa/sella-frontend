export { PageHome as default } from '~/pages/home';
// export { ComingSoonPage as default } from '~/pages/coming-soon';

// Force dynamic rendering to prevent build-time timeouts on Railway
export const dynamic = 'force-dynamic';
